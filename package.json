{
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "deploy": "git subtree push --prefix static origin gh-pages",
    "deploy:react": "cd example && yarn deploy",
    "pm": "cd prosemirror-view && yarn build",
    "serve": "node serve.js",
    "start": "cd example && yarn start"
  }
}